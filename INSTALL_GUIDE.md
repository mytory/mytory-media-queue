# Mytory YT-DLP 설치 안내

이 문서는 개발자가 아닌 사용자를 위한 설치 및 업데이트 안내입니다.

## 내려받기

배포자가 전달한 링크에서 사용 중인 운영체제에 맞는 파일 하나를 내려받으세요.

- **macOS (Apple Silicon·Intel)**: `.dmg`
- **Windows 64비트**: `.exe` 또는 `.msi`
- **Linux 64비트**: `.AppImage` 또는 `.deb`

앱은 Downloader와 FFmpeg/FFprobe를 함께 포함하므로 별도로 설치할 필요가 없습니다.

## macOS

1. `.dmg` 파일을 엽니다.
2. 앱을 `Applications` 폴더로 드래그합니다.
3. 처음 열 때 macOS가 개발자를 확인할 수 없다는 경고를 보이면:
   - Finder에서 앱을 Control-클릭(또는 오른쪽 클릭)하고 **열기**를 선택합니다.
   - 확인 창에서 다시 **열기**를 선택합니다.
4. 이후에는 일반 앱처럼 실행할 수 있습니다.

## Windows

1. `.exe` 설치 파일을 실행합니다. `.msi`를 받았다면 해당 파일을 실행합니다.
2. Microsoft Defender SmartScreen 경고가 표시되면, 배포자가 보낸 파일이 맞는지 확인한 뒤 **추가 정보** → **실행**을 선택합니다.
3. 설치가 끝나면 시작 메뉴 또는 바탕 화면에서 Mytory YT-DLP를 실행합니다.

## Linux

- `.deb`: 파일 관리자의 설치 기능을 사용하거나 배포판의 패키지 설치 도구로 설치합니다.
- `.AppImage`: 파일 속성에서 실행 권한을 허용한 뒤 실행합니다.

## 업데이트

앱 자체는 자동으로 업데이트되지 않습니다. 새 버전 안내를 받으면 새 설치 파일을 내려받아 기존 앱 위에 설치하세요. 기존 Download Queue와 내려받은 파일은 유지됩니다.

## 사용 전 확인

- 이 도구는 yt-dlp, FFmpeg 및 FFprobe를 포함합니다.
- Mytory YT-DLP는 yt-dlp, YouTube 또는 Google의 공식 제품이 아닙니다.
- 콘텐츠를 내려받기 전에 이용약관, 저작권 및 적용 법령에 따라 필요한 권한이 있는지 확인하세요.
- 로그인 쿠키 파일을 선택한 경우에도 앱은 이를 저장하거나 표시하지 않으며, Downloader 실행에만 전달합니다.

## 문제가 생겼을 때

- 앱이 열리지 않으면 위 운영체제별 최초 실행 절차를 다시 확인하세요.
- 다운로드가 실패하면 작업 목록에서 표시되는 진단 로그를 복사해 배포자에게 전달하세요. 쿠키 파일이나 계정 정보는 전달하지 마세요.
- 설치 파일이 의심스럽거나 배포자가 보낸 링크가 아닌 경우 실행하지 마세요.

## 배포자 확인 목록

지인에게 설치 파일을 전달할 때는 같은 버전의 다음 파일도 함께 제공하세요.

- `LICENSE`
- `THIRD_PARTY_NOTICES.md`
- 해당 버전의 완전한 소스 코드와 빌드 방법

이 프로젝트는 GPL-3.0-or-later로 배포되며, 번들 도구에는 각각의 라이선스 조건이 적용됩니다.
