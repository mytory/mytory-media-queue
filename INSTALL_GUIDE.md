# Mytory Media Queue 설치 안내

이 안내에 따라 앱을 설치하고 사용할 수 있습니다. 잘 모르겠다면 파일을 보내준 사람에게 물어보세요.

## 1. 내 컴퓨터에 맞는 파일 받기

- **Mac**을 사용한다면: 이름이 `.dmg`로 끝나는 파일
- **Windows**를 사용한다면: 이름이 `.exe` 또는 `.msi`로 끝나는 파일
- **Linux**를 사용한다면: 이름이 `.AppImage` 또는 `.deb`로 끝나는 파일

받은 파일이 맞는지 확신이 서지 않거나, 모르는 사람이 보낸 파일이라면 열지 마세요.

## 2. Mac에 설치하기

1. 받은 `.dmg` 파일을 두 번 클릭합니다.
2. 나타난 창에서 **Mytory Media Queue** 앱을 **Applications(응용 프로그램)** 폴더로 끌어다 놓습니다.
3. 응용 프로그램 폴더에서 Mytory Media Queue를 엽니다.

처음 열 때 “개발자를 확인할 수 없어 열 수 없습니다”라는 메시지가 나오면 다음을 하세요.

1. 응용 프로그램 폴더에서 Mytory Media Queue를 Control-클릭하거나 오른쪽 클릭합니다.
2. **열기**를 선택합니다.
3. 한 번 더 나타나는 창에서 **열기**를 선택합니다.

이 과정은 처음 한 번만 하면 됩니다.

## 3. Windows에 설치하기

1. 받은 `.exe` 또는 `.msi` 파일을 두 번 클릭합니다.
2. 화면 안내에 따라 설치합니다.
3. 설치가 끝나면 시작 메뉴 또는 바탕 화면에서 Mytory Media Queue를 엽니다.

“Windows에서 PC를 보호했습니다”라는 화면이 나오면, 파일을 보낸 사람이 맞는지 먼저 확인하세요. 맞다면 **추가 정보**를 누르고 **실행**을 선택합니다.

## 4. Linux에 설치하기

- `.deb` 파일: 파일을 두 번 클릭한 뒤 설치를 선택합니다.
- `.AppImage` 파일: 파일 속성에서 실행을 허용한 뒤 두 번 클릭합니다.

사용 중인 Linux에 따라 방법이 다를 수 있습니다. 설치가 안 되면 파일을 보내준 사람에게 문의하세요.

## 업데이트하기

앱은 자동으로 새 버전으로 바뀌지 않습니다. 새 버전을 받으면 새 설치 파일을 내려받아 설치하세요. 기존에 받은 파일과 작업 목록은 그대로 유지됩니다.

## 사용할 때 알아둘 점

- 이 앱은 동영상이나 오디오를 내려받는 도구입니다. 내려받아도 되는 콘텐츠인지 먼저 확인하세요.
- 로그인 쿠키 파일을 선택했다면, 그 파일은 다운로드를 실행할 때만 사용됩니다. 다른 사람에게 쿠키 파일이나 계정 정보를 보내지 마세요.
- 이 앱은 YouTube 또는 yt-dlp의 공식 앱이 아닙니다.

## 문제가 생기면

- 앱이 열리지 않으면 위의 설치 방법을 다시 확인하세요.
- 다운로드가 실패하면 앱의 작업 목록에서 보이는 오류 내용을 복사해 파일을 보내준 사람에게 전달하세요.
- 오류 내용에 쿠키 파일 경로나 계정 정보가 있으면 지우고 전달하세요.
